server {
    listen 80;
    server_name flashtalk.co.kr www.flashtalk.co.kr; # 본인 도메인으로 변경

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name flashtalk.co.kr www.flashtalk.co.kr; # 본인 도메인으로 변경

    ssl_certificate /etc/letsencrypt/live/flashtalk.co.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/flashtalk.co.kr/privkey.pem;

    location / {
        proxy_pass http://flashtalk-node:3000;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}